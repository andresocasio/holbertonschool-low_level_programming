#include <stdio.h>
/**
 * main - printf will print the text inside of the quotation
 *
 * Return: will wake the value of void = 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
